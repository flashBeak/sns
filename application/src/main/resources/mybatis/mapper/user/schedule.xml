<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ScheduleDAO">

	<select id="selectList" resultType="com.model.post.BoardVO">
 		SELECT *
		FROM board
		WHERE group_id = #{groupId}
		ORDER BY id DESC
	</select>

	<select id="selectListTotalCount" resultType="int">
 		SELECT
			COUNT(*) AS count
		WHERE group_id = #{groupId}
		FROM board
	</select>

	<select id="selectListByUserId" resultType="com.model.post.BoardVO">
 		SELECT g.*
		FROM board g
		JOIN board_member gm ON gm.board_id = g.id
		WHERE gm.user_id = #{userId}
		ORDER BY g.id DESC
	</select>

	<select id="select" parameterType="String" resultType="com.model.post.BoardVO">
		SELECT *
		FROM board
		WHERE id = #{id}
	</select>

	<insert id="insert" parameterType="com.model.post.BoardVO">
		INSERT INTO schedule (
			group_id, type
		<if test="categoryId != null and categoryId != ''">
			, category_id
		</if>
		) VALUES (
			#{groupId}, #{type}
		<if test="categoryId != null and categoryId != ''">
			, #{categoryId}
		</if>
		)
			
		<selectKey keyProperty="id" resultType="String">
        	SELECT LAST_INSERT_ID()
  		</selectKey>
	</insert>
	
	<update id="update" parameterType="com.model.post.BoardVO">
		UPDATE board SET
			id = #{id}
		<if test="type != null and type != ''">
			, type = #{type}
		</if>
		<if test="categoryId != null and categoryId != ''">
			, category_id = #{categoryId}
		</if>
		WHERE id = #{id}
	</update>

	<update id="delete" parameterType="String">
		DELETE FROM board
		WHERE id = #{id}
	</update>
</mapper>